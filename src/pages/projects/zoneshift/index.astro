---
import BaseLayout from "@/layouts/BaseLayout.astro";
import ZoneShiftDemo from "@/components/projects/zoneshift/ZoneShiftDemo";
---

<BaseLayout title="Zoneshift" description="A timezone shift planner focused on daily sleep alignment.">
  <section class="container mx-auto px-4 py-16 md:py-24">
    <div class="mx-auto flex max-w-3xl flex-col gap-6 text-muted-foreground">
      <p class="text-sm font-semibold uppercase tracking-[0.2em] text-muted-foreground">
        Project
      </p>
      <h1 class="text-3xl font-semibold text-foreground md:text-4xl">Zoneshift Planner</h1>
      <p>
        Zoneshift turns a small set of sleep parameters and real-world commitments into a day-by-day
        adaptation schedule. The demo below renders the computed view from Part 3 and surfaces the
        derived wake, sleep, and bright-light recommendations for each day. Toggle between home and
        target timezone projections to see how the same plan translates across locales.
      </p>
    </div>

    <div class="mt-12">
      <ZoneShiftDemo client:load />
    </div>
  </section>
</BaseLayout>
